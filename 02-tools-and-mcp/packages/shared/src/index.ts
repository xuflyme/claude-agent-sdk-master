/**
 * @02-tools-and-mcp/shared
 *
 * Shared business logic for 02-tools-and-mcp.
 *
 * Import specific modules via subpath exports:
 *   import { PromaAgent, type AgentEvent } from '@02-tools-and-mcp/shared/agent';
 *
 * Available modules:
 *   - agent: PromaAgent SDK wrapper, event handling, tool matching
 */

// Note: Import via subpath exports (e.g., '@02-tools-and-mcp/shared/agent')
// See package.json exports field for available subpaths
